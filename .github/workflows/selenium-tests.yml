name: BugLens QA Regression

on:
  push:
    branches:
      - main

  workflow_dispatch:

jobs:

  api-tests:
    name: Postman API Regression
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install Newman
        run: npm install -g newman

      - name: Run Postman API Tests
        run: newman run postman/BugLense.postman_collection.json


  selenium-tests:
    name: Selenium UI Regression
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
          cache: maven

      - name: Run Selenium Regression Tests
        run: mvn -f automation/pom.xml test -DbaseUrl="${{ secrets.BUGLENS_URL }}"

      - name: Upload Selenium Test Reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: BugLens-Selenium-Reports
          path: automation/target/surefire-reports/